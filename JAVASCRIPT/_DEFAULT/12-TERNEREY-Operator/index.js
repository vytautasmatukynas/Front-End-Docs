// Example 1: Simple conditional assignment
let age = 20;
let message = (age >= 18) ? "You are an adult" : "You are a minor";
console.log(message);

// Example 2: Using the result in a function call
let isLoggedIn = true;
let loginMessage = isLoggedIn ? "Welcome back!" : "Please log in";
console.log(loginMessage);
