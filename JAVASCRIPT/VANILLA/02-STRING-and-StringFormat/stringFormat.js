let firstName = "John";
let lastName = "Doe";
let age = 30;

// Using template literals to format a string
let formattedString = `${firstName} ${lastName} is ${age} years old.`;

console.log(formattedString);